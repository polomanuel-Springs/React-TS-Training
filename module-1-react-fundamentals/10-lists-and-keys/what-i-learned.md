# What I Learned

- I use `map` to turn array items into React elements.
- Each list element needs a `key`.
- A key must be unique among its siblings and stable between renders.
- A data ID is normally a better key than an index or random value.
