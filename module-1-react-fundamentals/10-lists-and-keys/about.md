# Lists and Keys

## What this topic is about

The array `map` method converts each item into a React element.

Each element in a rendered list needs a stable `key`. A key helps React identify which item corresponds to which element.

A unique ID from the data is normally a better key than an array index or a random number.

