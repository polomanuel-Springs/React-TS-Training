# What I Learned

- A pure component gives the same result for the same inputs.
- Render is when React calculates the UI.
- Commit is when React applies changes to the DOM.
- Components should not change external values during render.
- Strict Mode helps identify impure code during development.

