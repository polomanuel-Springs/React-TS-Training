# What I Learned

- JSX lets me write HTML-like syntax inside JavaScript.
- I use `{}` to display a JavaScript value in JSX.
- JSX tags must be closed.
- A component returns one parent element.
- Babel converts JSX into JavaScript.

