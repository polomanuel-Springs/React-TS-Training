# JSX

## What this topic is about

JSX is a syntax that looks like HTML and is written inside JavaScript.

Curly braces insert JavaScript values into JSX. JSX tags must be closed, and a component must return one parent element.

JSX is not understood directly by the browser, so Babel converts it into regular JavaScript.

