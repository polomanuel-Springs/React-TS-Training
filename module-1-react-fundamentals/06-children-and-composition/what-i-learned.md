# What I Learned

- `children` contains the content written inside a component.
- A wrapper component can display its `children`.
- Composition combines small components to build larger interfaces.
- Composition makes components more flexible and reusable.

