# One-Way Data Flow

## What this topic is about

React data moves from parent components to child components through props.

The parent owns the original data. The child receives and displays that data without modifying it.

This direction makes it easier to understand where a value comes from. Events and state changes will be studied later.

