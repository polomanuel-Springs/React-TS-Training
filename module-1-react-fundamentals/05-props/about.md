# Props

## What this topic is about

Props are values passed from a parent component to a child component.

Props are received in one object. Destructuring lets the child extract the properties it needs.

Props are read-only. The child uses them but should not modify them.

TypeScript can describe the expected prop names and value types. The simple browser example focuses only on the React behavior.

