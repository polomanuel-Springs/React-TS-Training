# What I Learned

- Props pass information from a parent to a child.
- Props arrive as an object.
- I can destructure props in the component parameter.
- Props are read-only.
- TypeScript can check the expected prop names and value types in a configured project.

