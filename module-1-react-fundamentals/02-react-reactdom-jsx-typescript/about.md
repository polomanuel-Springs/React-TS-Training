# React, ReactDOM, JSX, and TypeScript

## What this topic is about

These tools have different jobs:

- React creates and organizes the user interface.
- ReactDOM displays React content in the browser.
- JSX lets us write HTML-like syntax inside JavaScript.
- TypeScript adds type checking during development.

