# What I Learned

- React manages the user interface.
- ReactDOM connects React to the browser DOM.
- JSX is a simpler way to describe React elements.
- Babel converts JSX into JavaScript.
- TypeScript checks types during development, but Babel does not type-check them.

