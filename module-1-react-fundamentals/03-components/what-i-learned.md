# What I Learned

- A component is a reusable function that returns UI content.
- Component names start with a capital letter.
- I use a component with JSX, for example `<WelcomeMessage />`.
- I can combine small components to build a larger component.

